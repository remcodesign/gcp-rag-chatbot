{
  // rag-api — 100% strict TypeScript.
  //
  // This is a strict-types gate (no `any`) in addition to the module/export
  // gate from Domain 9. The DI factories, Firestore-shaped boundary, RAG
  // pipeline and SSE generator are all explicitly typed.
  //
  // NodeNext ESM: source files are `.ts` but import specifiers keep the `.js`
  // suffix (resolves to the on-disk `.ts`). `noEmit` keeps `tsc` a pure
  // type-checker; the runtime build uses `tsconfig.build.json` to emit `dist/`.
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "noImplicitAny": true,
    "noImplicitOverride": true,
    "noUncheckedIndexedAccess": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noEmit": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "lib/**/*.ts", "test/**/*.ts"]
}